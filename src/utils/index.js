import { checkEqualPassword, checkPassword, checkEmail, checkPhoneNumber, checkUsername } from './handleSubmit';

export { checkEqualPassword, checkPassword, checkEmail, checkPhoneNumber, checkUsername };
